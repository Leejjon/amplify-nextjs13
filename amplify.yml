version: 1
applications:
  - appRoot: app
    frontend:
      phases:
        preBuild:
          commands:
            - cd ..
            - yarn install
        build:
          commands:
            - pwd
            - yarn build
            - pwd
        postBuild:
          commands:
            - shopt -s dotglob
            - mv -f app/.next/standalone/apps/web/* .next/standalone
            - rm -rf app/.next/standalone/apps
      artifacts:
        baseDirectory: app/.next/
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
